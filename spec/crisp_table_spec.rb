require "spec_helper"

describe CrispTable do
  it "has a version number" do
    expect(CrispTable::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
